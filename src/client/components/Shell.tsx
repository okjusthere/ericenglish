import { BarChart3, BookOpen, CalendarCheck, Camera, ClipboardCheck, MessageSquareText, Mic2, NotebookTabs, Settings, Sparkles, TimerReset } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
const links=[['/today','Today',CalendarCheck],['/review','Review',TimerReset],['/speak','Speak',Mic2],['/write','Write',MessageSquareText],['/capture','Capture',Camera],['/prepare','Prepare',Sparkles],['/patterns','Patterns',NotebookTabs],['/library','Library',BookOpen],['/progress','Progress',BarChart3],['/assessment','Assessment',ClipboardCheck],['/settings','Settings',Settings]] as const;
export function Shell(){return <div className="app-shell"><aside className="sidebar"><NavLink to="/today" className="brand"><span>E</span><div>Eric English<small>OPERATING SYSTEM</small></div></NavLink><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-note"><span className="live-dot"/>Private workspace<small>America / New York</small></div></aside><main className="main"><header className="mobile-head"><NavLink to="/today" className="brand"><span>E</span><div>English OS</div></NavLink><div className="date-pill">12-week fieldwork</div></header><Outlet/></main><nav className="mobile-nav">{links.slice(0,5).map(([to,label,Icon])=><NavLink key={to} to={to}><Icon size={20}/><span>{label}</span></NavLink>)}</nav></div>}
