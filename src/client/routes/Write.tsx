import { useState } from 'react';import { ArrowRight, Clock } from 'lucide-react';import { post } from '../lib/api';import { Button, Eyebrow, Panel } from '../components/ui';
type Result={id:string;correct:string;natural:string;polished:string;reasons:string[];phraseUpgrades:string[]};
const tasks=[['text_30s','30 sec','Ask a listing agent whether a space is available and confirm the asking rent.'],['followup_2m','2 min','Follow up after a showing and request clarification on CAM and property taxes.'],['explanation_5m','5 min','Compare two options and recommend one to a client with clear reasons.']] as const;
export function Write(){const [task,setTask]=useState<(typeof tasks)[number]>(tasks[0]);const [text,setText]=useState('');const [result,setResult]=useState<Result>();const [loading,setLoading]=useState(false);const submit=()=>{setLoading(true);void post<Result>('/api/writing/evaluate',{taskType:task[0],prompt:task[2],text}).then(setResult).finally(()=>setLoading(false));};return <div className="page"><div className="page-heading split"><div><Eyebrow>WRITING LAB</Eyebrow><h1>Your draft first.<br/><em>Then three useful levels.</em></h1></div><div className="task-tabs">{tasks.map((x)=><button className={task[0]===x[0]?'active':''} onClick={()=>setTask(x)} key={x[0]}><Clock/>{x[1]}</button>)}</div></div><Panel className="writing-editor"><p>{task[2]}</p><textarea value={text} onChange={(e)=>setText(e.target.value)} placeholder="Write without AI help first…"/><div className="editor-foot"><span>{text.trim()?text.trim().split(/\s+/).length:0} words</span><Button disabled={text.length<3||loading} onClick={submit}>{loading?'Evaluating…':'Evaluate my draft'} <ArrowRight/></Button></div></Panel>{result&&<div className="rewrite-grid">{(['correct','natural','polished'] as const).map((level)=><Panel key={level}><Eyebrow>{level.toUpperCase()}</Eyebrow><p>{result[level]}</p></Panel>)}<Panel className="wide"><Eyebrow>WHY THESE CHANGES</Eyebrow><ol>{result.reasons.map((r)=><li key={r}>{r}</li>)}</ol><div className="chips">{result.phraseUpgrades.map((p)=><span key={p}>{p}</span>)}</div><Button onClick={()=>void post(`/api/writing/${result.id}/create-units`)}>Create review units</Button></Panel></div>}</div>}
