Version: 1.0.0

Identify likely names or context-sensitive identifiers that deterministic redaction missed. Never repeat raw personal data in the response. Addresses may remain only when the caller explicitly permits them. Return only schema-conforming JSON.
