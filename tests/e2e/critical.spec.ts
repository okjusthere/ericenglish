import { expect,test } from '@playwright/test';
test.describe.configure({mode:'serial'});
async function mockSpeech(page:import('@playwright/test').Page){await page.addInitScript(()=>{const spoken:string[]=[];Object.defineProperty(window,'__spokenEnglish',{configurable:true,value:spoken});class Utterance{text:string;lang='';rate=1;pitch=1;voice:SpeechSynthesisVoice|null=null;onstart:(()=>void)|null=null;onend:(()=>void)|null=null;onerror:(()=>void)|null=null;constructor(text:string){this.text=text}}Object.defineProperty(window,'SpeechSynthesisUtterance',{configurable:true,value:Utterance});Object.defineProperty(window,'speechSynthesis',{configurable:true,value:{getVoices:()=>[],cancel:()=>undefined,resume:()=>undefined,speak:(utterance:Utterance)=>{spoken.push(utterance.text);utterance.onstart?.();utterance.onend?.();}}});});}
test('first-run bootstrap and core learning paths',async({page})=>{await page.goto('/today');const bootstrap=page.getByRole('button',{name:/Bootstrap English OS/i});if(await bootstrap.isVisible().catch(()=>false))await bootstrap.click();await expect(page.getByRole('heading',{name:/Today, make it/i})).toBeVisible({timeout:15_000});await page.goto('/review');await page.getByRole('button',{name:/Reveal answer/i}).click();await page.getByRole('button',{name:/Good/i}).click();await page.goto('/write');await page.getByPlaceholder(/Write without AI help/i).fill('Can the price lower?');await page.getByRole('button',{name:/Evaluate my draft/i}).click();await expect(page.getByText('NATURAL',{exact:true})).toBeVisible();await page.goto('/capture');await page.getByPlaceholder(/I wanted to say/i).fill('I wanted to ask whether the rent could change.');await page.getByRole('button',{name:'Capture text'}).click();await expect(page.getByText('NATURAL REWRITE')).toBeVisible();await page.goto('/prepare');await page.getByRole('button',{name:/Build my preparation/i}).click();await expect(page.getByText('PHONE CHEAT SHEET')).toBeVisible();await page.getByPlaceholder(/Outcome, next step/i).fill('We confirmed a tour and the next step.');await page.getByRole('button',{name:/Save review/i}).click();await expect(page.getByText('NATURAL RETELLING',{exact:true})).toBeVisible();await page.goto('/drill');await page.getByRole('button',{name:/Reveal.*answer/i}).click();await page.getByRole('button',{name:/Produced it/i}).click();await page.goto('/progress');await expect(page.getByText('ACTIVE RECALL ACCURACY')).toBeVisible();await page.goto('/settings');await expect(page.getByText('Own the complete record')).toBeVisible();const downloadPromise=page.waitForEvent('download');await page.getByRole('button',{name:'JSON'}).click();expect((await downloadPromise).suggestedFilename()).toContain('eric-english-os');});
test('text speaking session persists a turn',async({page})=>{await page.goto('/speak');const start=page.getByRole('button',{name:/Start fluency/i}).first();await start.click();await page.getByPlaceholder(/Type a turn/i).fill('Could you clarify whether CAM is included?');await page.keyboard.press('Enter');await expect(page.getByText(/I can check that/i)).toBeVisible();});
test('core learning paths expose and play US English audio',async({page})=>{await mockSpeech(page);const plays=()=>page.evaluate(()=>(window as unknown as {__spokenEnglish:string[]}).__spokenEnglish.length);await page.goto('/review');await page.getByRole('button',{name:/Reveal answer/i}).click();await expect(page.getByRole('button',{name:/Replay natural answer/i})).toBeVisible();await expect.poll(plays).toBeGreaterThan(0);await page.goto('/drill');await page.getByRole('button',{name:/Reveal & hear answer/i}).click();await expect(page.getByRole('button',{name:/Replay model answer/i})).toBeVisible();await expect.poll(plays).toBeGreaterThan(0);await page.goto('/library');await page.locator('.unit-table:not(.head)').first().click();await page.getByRole('button',{name:/Hear pronunciation/i}).click();await expect.poll(plays).toBeGreaterThan(0);});
